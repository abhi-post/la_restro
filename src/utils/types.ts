export type CreateUserParams = {
    username: string;
    password: string;
}

export type UpdateUserParams = {
    username: string;
    password: string;
}

export type CreateShopParams = {
    shop_name: string;
    mobile_no: number;
    email_id: string;
    title_name: string;
}